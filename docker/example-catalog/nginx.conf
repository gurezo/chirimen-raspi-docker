# Static host for apps/example-catalog production build.
# Catalog is a single page with no client router.
server {
    listen 4174;
    listen [::]:4174;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
