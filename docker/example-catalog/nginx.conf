# Static host for apps/catalog production build.
# Catalog is a single page with no client router.
server {
    listen 4200;
    listen [::]:4200;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
