{
  "name": "protocol",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/protocol/src",
  "projectType": "library",
  "tags": [
    "type:lib",
    "scope:shared",
    "layer:protocol"
  ],
  "// targets": "to see all targets run: nx show project protocol --web",
  "targets": {
    "test": {
      "executor": "@nx/vite:test",
      "outputs": [
        "{options.reportsDirectory}"
      ],
      "options": {
        "reportsDirectory": "../../coverage/libs/protocol"
      }
    }
  }
}
